#주사위 굴리는 프로그램
import random #임의의 값을 얻기 위해 사용하는 random 모듈을 가져온다.

n = random.randint(1,6) #1에서 6까지의 정수 중 하나를 뽑는다.
print("결과 : ", n)
n = random.randint(1, 6)
print("결과 : ", n)
n = random.randint(1, 6)
print("결과 : ", n)